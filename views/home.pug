extends layouts/main
include mixins/videoBlock

block content 
    .videos 
        //- each video in videos 
        each item in videos 
            //- p #{item.fileUrl}
            +videoBlock({
                id : item.id,
                title : item.title,
                videoFile : item.fileUrl,
                views : item.views
            })